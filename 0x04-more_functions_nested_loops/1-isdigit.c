#include "main.h"
/**
 * _isdigit - funtion count 0 to the 9
 * @c: number integer
 * Return: 1  true or 0 false
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
	return (1);
	}
	else
	return (0);
}

