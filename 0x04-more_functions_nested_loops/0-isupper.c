#include "main.h"
/**
 * _isupper - function that checks for uppercase characte
 * @c: tour character
 * Return: 1 true or 0 false
 */
int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
else
return (0);
}
